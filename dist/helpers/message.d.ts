import { MessageEmbed } from 'discord.js';
export declare function createEmbed(title: string, message: string): {
    embeds: MessageEmbed[];
};
export declare function createErrorEmbed(message: string): {
    embeds: MessageEmbed[];
};
